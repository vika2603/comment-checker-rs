name: Build Tree-Sitter Parsers

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Parser release version (e.g. parsers-v1)'
        required: true
        default: 'parsers-v1'

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            suffix: darwin-arm64
            cc_flags: "-target arm64-apple-macos11"
          - os: macos-latest
            suffix: darwin-x86_64
            cc_flags: "-target x86_64-apple-macos11"
          - os: ubuntu-latest
            suffix: linux-x86_64
            cc_flags: ""
          - os: ubuntu-24.04-arm
            suffix: linux-aarch64
            cc_flags: ""
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Build grammar .so files
        run: |
          chmod +x scripts/build-parsers.sh
          ./scripts/build-parsers.sh "${{ matrix.suffix }}" "${{ matrix.cc_flags }}"

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: parsers-${{ matrix.suffix }}
          path: build/parsers/*.so

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: all-parsers
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd all-parsers
          sha256sum *.so > checksums.sha256
          cat checksums.sha256

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          files: |
            all-parsers/*.so
            all-parsers/checksums.sha256
