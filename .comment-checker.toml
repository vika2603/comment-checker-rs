# comment-checker's own config -- dogfooding the tool
#
# Patterns matched against stripped comment content (prefix removed).

allowlist = [
  # Allowlist section headers (organizing long pattern lists)
  "^(Linter|Rust|Compiler|Type|Shebang|Encoding|License|BDD|Region)",

  # Test explanation comments (line N, buffer, etc.)
  "^\"",
  "\\bline \\d+\\b",
  "merged$",
  "^Both ranges",
  "^Write tool",

  # Test assertion intent comments (parser_test.rs)
  "^The fixture has several comments",
  "^String literals inside source code must not be returned",

  # Grammar cache lifetime explanation (parser_test.rs / snapshot_test.rs)
  "^Leak the cache so the Library",
  "^Multiple tests run in the same process",
  "which invalidates the function pointers",

  # snapshot_test.rs helper doc comment
  "^Returns None when the nvim parser",
  "allowing the caller to skip the test gracefully",

  # Test assertion labels (parser_test.rs)
  "^Comments that should be allowed",
  "^Ensure \"regular comment\" is not in the allowed list",
  "^Ensure \"TODO: fix this\" is not in the allowed list",

  # Accurate doc comment for is_python_docstring function
  "Returns true if `node` is a Python docstring",
  "`expression_statement` that is the first non-comment statement",
  "module, class body, or function body",

  # Dynamic grammar loading doc comments (so_file_name, symbol_name, grammar_name methods)
  "\\.so",
  "^The C symbol exported",
  "^The grammar name used in download URLs",
  "^All distinct grammar names",

  # Temporary stub in parser/mod.rs until Task 7
  "TODO\\(task-7\\)",
  "cannot be parsed",

  # GrammarCache internals (grammar.rs)
  "already-cached Language without triggering",
  "raw fn ptr",
  "verified working with libloading",
  "^Validate ABI",

  # Config loading doc comment (config.rs)
  "^Load config from the first location",

  # Grammar resolution chain (grammar.rs)
  "^Resolve a grammar.*search dirs.*download",
  "^Build the ordered list of directories",

  # Download and integrity verification (grammar.rs)
  "atomic write.*tmp.*verify.*rename",
  "^Verify SHA256",
  "^Atomic write.*tmp.*rename",

  # Linter directives in shell scripts
  "^shellcheck",
]
